name: Bug report
description: Report a reproducible Apple Reminders plugin problem without sharing private Reminder data.
title: "[Bug]: "
labels:
  - bug
body:
  - type: markdown
    attributes:
      value: |
        Thanks for helping improve Apple Reminders.

        **Privacy first:** do not attach real Reminder titles, notes, URLs, identifiers, screenshots, databases, journals, caches, backups, or local paths. Replace sensitive values with small synthetic examples. Read the [privacy policy](https://github.com/Oscar-V4/apple-reminders/blob/main/PRIVACY.md) before submitting.

  - type: dropdown
    id: area
    attributes:
      label: Affected area
      options:
        - Installation or first use
        - Core reads or writes
        - Native sections or tags
        - Native image or URL attachments
        - Recently Deleted or recovery
        - Diagnostics
        - Skills or documentation
        - Packaging or release artifact
    validations:
      required: true

  - type: input
    id: plugin-version
    attributes:
      label: Plugin version or commit
      description: Use the installed version, tag, or exact commit SHA.
      placeholder: "0.4.0 or abc1234"
    validations:
      required: true

  - type: input
    id: macos-version
    attributes:
      label: macOS version
      placeholder: "14.7"
    validations:
      required: true

  - type: dropdown
    id: mac-hardware
    attributes:
      label: Mac hardware
      options:
        - Apple silicon
        - Intel
        - Not sure
    validations:
      required: true

  - type: dropdown
    id: permission-state
    attributes:
      label: Reminders permission state before the failure
      description: Do not reset permissions just to answer this question.
      options:
        - First use; macOS had not asked yet
        - Granted
        - Denied
        - Changed in System Settings
        - Not sure
    validations:
      required: true

  - type: input
    id: python-version
    attributes:
      label: Python version and installation source
      description: Report the version selected by the plugin or its content-free packaging diagnosis, plus Homebrew, python.org, or another source. A terminal PATH can differ from a Finder-launched Codex PATH.
      placeholder: "Python 3.12.10 from Homebrew"
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Reproduction steps
      description: Use a disposable list and synthetic Reminder content where possible.
      placeholder: |
        1. Install from ...
        2. Start a new Codex task.
        3. Ask Codex to ... using synthetic data.
        4. Observe ...
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: Actual behavior
      description: Include only a redacted, bounded status or Receipt. Do not paste full Doctor output or private data.
    validations:
      required: true

  - type: textarea
    id: additional-context
    attributes:
      label: Additional sanitized context
      description: Optional. Describe whether retrying, a fresh exact read, or restarting Codex changed the result.

  - type: checkboxes
    id: privacy
    attributes:
      label: Privacy and safety confirmation
      options:
        - label: I removed real Reminder content, identifiers, URLs, screenshots, databases, logs, backups, and local paths.
          required: true
        - label: I searched existing issues and tested the latest available version or commit.
          required: true
