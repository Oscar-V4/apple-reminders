# Apple Reminders v0.4.0 release evidence

This content-free receipt records the release gates exercised for the
community-built Apple Reminders plugin. It intentionally contains no Reminder
titles, notes, list or account names, attachment metadata, private paths, or
opaque writable references.

## Tagged release identity

| Field | Verified value |
| --- | --- |
| Tag | [`v0.4.0`](https://github.com/Oscar-V4/apple-reminders/releases/tag/v0.4.0) |
| Tagged commit | [`f8eb9e25e6eec486cbf99650bc74ec57f1d5de65`](https://github.com/Oscar-V4/apple-reminders/commit/f8eb9e25e6eec486cbf99650bc74ec57f1d5de65) |
| Final CI | [GitHub Actions run 33257175249](https://github.com/Oscar-V4/apple-reminders/actions/runs/33257175249), Python 3.11 and 3.12 passed |
| Release archive | [`apple-reminders-0.4.0.zip`](https://github.com/Oscar-V4/apple-reminders/releases/download/v0.4.0/apple-reminders-0.4.0.zip), 1,256,730 bytes |
| SHA-256 | `a0f53ae4053053abe75541e68ae35b01f904ff4a013b99b14e686b95fa1355da` |
| Checksum asset | [`apple-reminders-0.4.0.zip.sha256`](https://github.com/Oscar-V4/apple-reminders/releases/download/v0.4.0/apple-reminders-0.4.0.zip.sha256) |

The annotated remote tag resolves to the commit that was `main` when the
pre-release was created. Later documentation-only `main` commits do not move
the tag or change its archive. The two locally built archives compared
byte-for-byte equal. The published archive was then downloaded again, verified
with the published checksum, compared byte-for-byte with the local candidate,
and accepted by the repository archive auditor as the exact allowlisted
package.

## Automated release checks

- The final tagged tree passed 615 synthetic unit, contract, integration,
  workflow, corruption, and packaging tests locally in 19.673 seconds.
- The tagged commit's CI passed on Python 3.11 and 3.12.
- Plugin, skill/eval metadata, Minis export, source allowlist, Python compile,
  EventKit request validation, Objective-C syntax, plist, deterministic ZIP,
  and post-check source-artifact gates passed.
- The package contains 55 allowlisted files. Tests, development scripts, local
  state, logs, caches, receipts, and ignored artifacts are not archive members.

## Exact-tag clean install

On 2026-08-29, the README's public commands were run against an empty temporary
Codex home rather than the development checkout:

```bash
codex plugin marketplace add Oscar-V4/apple-reminders --ref v0.4.0
codex plugin add apple-reminders@oscar-v4-reminders
```

The cloned marketplace resolved to the tagged commit above. Codex reported
installed plugin version `0.4.0`. With a Finder-like minimal `PATH`, the
installed launcher selected `/opt/homebrew/bin/python3` version 3.14.6 from its
reviewed fixed-location fallbacks, and the installed MCP reported:

| Gate | Result |
| --- | --- |
| MCP server | `apple-reminders-local` version `0.4.0` |
| Public tool discovery | exactly 15 unique tools |
| First bounded Core read | `verified`, limit 1 |
| Content-free packaging diagnosis | `verified`, `overall=ready` |
| Private Reminder content printed | no |

Machine-readable receipt:
[`v0.4.0-exact-install.json`](v0.4.0-exact-install.json), SHA-256
`b73c53a149ce90830d8fe30b0efad6ce83b2fa5e03ffc3dcc2b4bbfd44ac4778`.

The first Core read confirmed the real permission/runtime path. Its returned
user-authored fields were inspected only by the validation process and were not
printed or copied into this receipt.

## Live Apple Reminders smoke

The smoke harness was run again directly from the isolated `v0.4.0` marketplace
checkout after the release was published. It completed all 23 live steps
against one uniquely named synthetic Reminder List and removed only the
reserved synthetic records it created. The metadata-bound machine receipt is
[`v0.4.0-live-smoke.json`](v0.4.0-live-smoke.json), SHA-256
`74a770ec4dc92b5a318dfa5009b64e3884d1d866ddfc7f291226ca9e2232a751`;
its referenced raw 23-line process output is
[`v0.4.0-live-smoke.txt`](v0.4.0-live-smoke.txt), SHA-256
`5608e8e508bd8e7a815097b54a0fbbcbfba7fb69409d501ae26162d3ab9067ed`.
Both contain only tag/synthetic-scope metadata, step names, pass status, and
wall-time latency—no title, list, account, attachment, or opaque identity.

This is evidence for the tested Mac and moment, not a compatibility promise for
every macOS build, Apple account, or device. The complete first-use flow has not
yet been repeated on a genuinely fresh macOS TCC profile.

## Data-free release benchmark

The benchmark was rerun from a clean `main` at the exact tagged commit on
2026-08-29T14:27:53Z. Environment: Darwin 25.5.0, arm64, Python 3.14.6. Timings
are end-to-end subprocess wall time, include process startup, do not control
host load, and are not cross-machine scores. Normal cases used 3 warmups and 15
measured samples; helper-build cases used 1 warmup and 5 measured samples.

| Gate | Samples | p95 | Budget | Result |
| --- | ---: | ---: | ---: | --- |
| MCP initialize + tools/list | 15 | 102.101 ms | 300 ms | passed |
| MCP Doctor route | 15 | 503.499 ms | 1,500 ms | passed |
| Cached EventKit helper build | 5 | 79.218 ms | 350 ms | passed |
| Fresh EventKit helper build | 5 | 390.543 ms | 2,500 ms | passed |

Machine-readable benchmark:
[`v0.4.0-benchmark.json`](v0.4.0-benchmark.json), SHA-256
`ff26e077b63fa03c69756a7b2383ed962b26a1177f0e102119275c384b11b694`.

The benchmark requested no TCC access, read no Reminder rows, never executed a
native helper, and used an isolated temporary home for Doctor. It compiled the
EventKit helper and exercised only validation-safe paths.

## Separate-agent AI audit trail

A separately orchestrated WebGPT Agent Pro audit of the hardening stack
identified an age-retention bug in unresolved idempotency fences in
[PR #19](https://github.com/Oscar-V4/apple-reminders/pull/19#pullrequestreview-5057306058).
The agent identity is the maintainer's attestation; the owner-authored public
review proves the finding and response, not which agent produced it. Treat this
as cross-agent AI review evidence, not a third-party or independent human
review. The fix was added before the PR's fresh CI and merge. Subsequent PRs
and the tagged release passed the gates above.

## Remaining public-launch work

- Use the rights-owned [1200×630 launch cover](../launch/assets/apple-reminders-v0.4.0-cover.png)
  for Showcase/SNS and set it as the repository social preview when approved.
- Capture a synthetic-data-only 60–90 second demo.
- Repeat the first permission allow/deny/retry journey on a genuinely fresh
  macOS TCC profile.
- The official OpenAI community page currently links “Submit a project” to the
  Showcase gallery, but no public submission form or control is exposed there;
  the former standalone form returns 403. Recheck the official route instead
  of guessing a submission endpoint.
- Treat any future Showcase submission, Developer Forum posting, and SNS
  posting as separate representational actions. A submitted project is not a
  featured or endorsed project unless OpenAI publishes it as such.
